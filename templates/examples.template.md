<!--
EXAMPLES 模板。所有示例必须为虚构、泛化或脱敏内容，不使用未经核实的真实品牌数据。
示例必须与 Skill 当前触发条件和边界一致，不得通过示例扩大职责。
-->
# 使用示例（<中文名> / <name>）

> 所有示例均为虚构、泛化或脱敏内容；不复制某个具体 Skill 的业务逻辑。

## 1. 典型正确调用（至少 3 个）

### 示例 A
- 输入：<用户语料或数据>
- 预期处理逻辑：<Skill 应如何理解并路由>
- 预期输出结构：<章节/字段>
- 标记示例：<事实 / 推断 / 未知 / 建议>

### 示例 B
- 输入：<...>
- 预期处理逻辑：<...>
- 预期输出结构：<...>
- 标记示例：<...>

### 示例 C
- 输入：<...>
- 预期处理逻辑：<...>
- 预期输出结构：<...>
- 标记示例：<...>

## 2. 信息不足但可以合理继续
- 输入：<缺失某项但可合理假设>
- 预期：标"假设：…"后继续，不静默编造

## 3. 必须追问
- 输入：<缺失会实质影响结果>
- 预期：集中询问一次，只问关键项

## 4. 不应调用本 Skill（反例）
- 输入：<属于相邻 Skill 或超出边界>
- 预期：说明应交由哪个 Skill，不执行

## 5. 一致性要求
- 所有数字内部可加总、可核对（如 正6/中12/负2=20）。
- 不扩大 Skill 职责。
- 不泄露隐私。
